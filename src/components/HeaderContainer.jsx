import { Component } from "react";

class Header extends Component {
    render() {
        return (
            <header class="section">
                <p class="title">
                    Basic Todo App
                </p>
                <p class="subtitle">
                    Using redux
                </p>
            </header>
        )
    }
}

export default Header;